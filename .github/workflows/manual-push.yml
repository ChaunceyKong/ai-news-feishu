name: Manual News Push

on:
  workflow_dispatch:  # 允许手动触发
    inputs:
      dry-run:
        description: 'Dry run mode (no actual message sending)'
        required: false
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'
      message-type:
        description: 'Message type'
        required: false
        default: 'mixed'
        type: choice
        options:
          - 'text'
          - 'card'
          - 'mixed'

  schedule:
    # 每天北京时间9点运行 (UTC+8 对应 UTC 1:00)
    - cron: '0 1 * * *'

jobs:
  push-news:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Configure environment
      run: |
        # 复制环境变量模板
        cp .env.example .env
        # 替换环境变量值
        sed -i "s|your_app_id_here|${{ secrets.LARK_APP_ID }}|g" .env
        sed -i "s|your_app_secret_here|${{ secrets.LARK_APP_SECRET }}|g" .env
        sed -i "s|ou_xxx|${{ secrets.LARK_RECEIVER_ID }}|g" .env
        # 根据输入设置运行模式
        if [ "${{ github.event.inputs.dry-run }}" = "true" ]; then
          sed -i "s|DRY_RUN=false|DRY_RUN=true|g" .env
        fi

    - name: Create data directory
      run: mkdir -p data

    - name: Cache news database
      uses: actions/cache@v4
      with:
        path: data/news.db
        key: news-db-${{ runner.os }}
        restore-keys: |
          news-db-${{ runner.os }}

    - name: Test configuration
      env:
        LARK_APP_ID: ${{ secrets.LARK_APP_ID }}
        LARK_APP_SECRET: ${{ secrets.LARK_APP_SECRET }}
        LARK_RECEIVER_ID: ${{ secrets.LARK_RECEIVER_ID }}
      run: |
        # 验证Secrets是否配置（不输出具体值）
        echo "LARK_APP_ID: ${LARK_APP_ID:+configured}"
        echo "LARK_APP_SECRET: ${LARK_APP_SECRET:+configured}"
        echo "LARK_RECEIVER_ID: ${LARK_RECEIVER_ID:+configured}"
        # 测试配置
        python -m src test-config

    - name: Run news push
      env:
        LARK_APP_ID: ${{ secrets.LARK_APP_ID }}
        LARK_APP_SECRET: ${{ secrets.LARK_APP_SECRET }}
        LARK_RECEIVER_ID: ${{ secrets.LARK_RECEIVER_ID }}
      run: |
        CMD_ARGS="run"
        if [ "${{ github.event.inputs.dry-run }}" = "true" ]; then
          CMD_ARGS="$CMD_ARGS --dry-run"
        fi
        if [ -n "${{ github.event.inputs.message-type }}" ]; then
          CMD_ARGS="$CMD_ARGS --message-type ${{ github.event.inputs.message-type }}"
        fi
        python -m src $CMD_ARGS

    - name: Upload logs on failure
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: error-logs
        path: |
          data/*.log
          .env
        retention-days: 7